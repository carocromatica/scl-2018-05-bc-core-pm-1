

    function encode() {
        let str = document.getElementById("mensaje").value; 
        let key = document.getElementById("key").value;    
    }

    function decode() {
        let str = document.getElementById("mensaje").value; 
        let key = document.getElementById("key").value;    
    }
